# Fake-News-Detector
Fake News Detection project built using Machine Learning and Natural Language Processing (NLP). The model analyzes news text and predicts whether the news is real or fake using text preprocessing, TF-IDF vectorization, and classification algorithms trained on a labeled dataset.
